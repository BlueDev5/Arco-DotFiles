const hello = 'I am not used';

doesNotExist.nope(`I am never imported`);

class widget extends React.Component {
  jared() {
    this.something = 'Hey listen!';
    return false;
  }
}

const jared = function () { };
const best = () => { };

console.log(jared, best);
